% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow.R
\name{graft_streams}
\alias{graft_streams}
\title{List decision streams and their current decisions}
\usage{
graft_streams(
  store,
  purpose = NULL,
  max_streams = 1000L,
  max_decisions = 1000L,
  max_metadata_bytes = 1024^2
)
}
\arguments{
\item{store}{A store returned by \code{\link[=graft_store]{graft_store()}} or
\code{\link[=graft_store_postgres]{graft_store_postgres()}}.}

\item{purpose}{Optional purpose. When supplied, only streams whose current
decision was recorded for this purpose are returned.}

\item{max_streams}{Maximum decision streams to inspect.}

\item{max_decisions}{Maximum decision records to inspect per stream.}

\item{max_metadata_bytes}{Maximum aggregate decision metadata bytes across
all streams.}
}
\value{
A list of \link{Decision} values, one per stream: each stream's current
decision, ordered by stream name. Check \verb{@action} for \code{"accept"} or
\code{"withdraw"}.
}
\description{
Find the decision streams in a store without keeping a separate catalog.
Each stream's journal is read and verified the same way as
\code{\link[=graft_history]{graft_history()}}, and its latest decision is returned. Like
\code{\link[=graft_history]{graft_history()}}, this reads decision records only: it returns no artifact
content and needs no eligibility decision. Use \code{\link[=graft_recall]{graft_recall()}} to read the
accepted evidence. The host application still decides who may see stream
names, actors, and reasons.
}
\details{
Decisions carry no timestamps, so streams are ordered by name, compared
bytewise.
}
