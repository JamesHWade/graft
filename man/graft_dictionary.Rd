% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dictionary.R
\name{graft_dictionary}
\alias{graft_dictionary}
\title{Discover the public data dictionary at an accepted boundary}
\usage{
graft_dictionary(source, table = NULL, field = NULL, limit = 100L, offset = 0L)
}
\arguments{
\item{source}{An initialized \code{GraftStore} or immutable \code{GraftView} with a
data-dict contract. Views retain their historical contract and receipt.}

\item{table}{Optional dictionary table name.}

\item{field}{Optional public column name; requires \code{table}.}

\item{limit}{Maximum entries to return, from 1 to 100.}

\item{offset}{Number of entries to skip, from 0 to 1,000,000.}
}
\value{
A list with \code{result}, \code{truncated}, \code{limit}, and a canonical \code{receipt}.
\code{result} contains an \code{entries} data frame, \code{total}, and \code{next_offset}
(\code{NULL} on the last page). Entries have \code{kind}, \code{table}, \code{field}, \code{name},
\code{value}, \code{semantics}, and \code{text_truncated} columns. Compound values are JSON.
}
\description{
\code{graft_dictionary()} exposes the accepted data-dict contract without record
values, examples, observed ranges, source locators, or restricted columns.
Public descriptive prose is not content-scrubbed; authors must keep private
information out of public descriptions and glossary entries.
}
\details{
Entries follow document order, then adapter semantics. Each string cell is
capped at 2,000 characters. \code{text_truncated} marks clipped rows, and the outer
\code{truncated} flag also reports remaining pages. Narrow the selection or use
\code{next_offset} to retrieve another page. Selection uses dictionary names.
Dataset and glossary context is included with every selection; table and
column context is scoped. Relationships are included only when all endpoints
are public and at least one endpoint matches the selection. Only resolved
endpoint pairs and cardinality are shown: pairs do not encode join operators
or aliases, so discovery does not reconstruct a join expression.
Assertions require resolved public column references; dataset assertions
and assertions without resolved references are omitted.

\code{supported} entries describe enforced contract properties. \code{descriptive}
entries are metadata, not executable assertions or acceptance constraints.
\code{unsupported} entries explain semantics outside Graft's supported profile.
Discovery is a generic read for \code{\link[=graft_verify]{graft_verify()}}: an explicit matching
quotation is required for cited evidence, and it is never calculation evidence.
}
