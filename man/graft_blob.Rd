% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blobs.R
\name{graft_blob}
\alias{graft_blob}
\title{Stage bytes as a content-addressed blob}
\usage{
graft_blob(store, x, media_type)
}
\arguments{
\item{store}{An initialized, writable \code{GraftStore}.}

\item{x}{A raw vector, or the path to one file.}

\item{media_type}{The media type of the bytes, such as \code{"image/png"}.}
}
\value{
A one-row data frame with \code{id}, \code{media_type}, and \code{size_bytes}.
}
\description{
A contract that declares a \code{graft_blob} table can hold files beside its
records. \code{graft_blob()} writes the bytes into the store's blob directory,
named by their SHA-256 digest, and returns the one-row \code{graft_blob} record
that refers to them. Supply that row to \code{\link[=graft_plan]{graft_plan()}} with the records
that reference its \code{id}, so the bytes and the references are reviewed and
committed together. Identical bytes are stored once.
}
\details{
The \code{graft_blob} table must have a string \code{id} primary key, a required
string \code{media_type}, and a required numeric \code{size_bytes}. Planning fails
when a blob row's bytes are missing, differ in size or digest, or when a
committed blob would change.
}
\examples{
dictionary <- system.file(
  "extdata",
  "artifacts.data-dict.json",
  package = "graft",
  mustWork = TRUE
)
store <- graft_open(graft_schema(dictionary), okf = "disabled")
graft_blob(store, charToRaw("<p>Hello</p>"), "text/html")
graft_close(store)
}
