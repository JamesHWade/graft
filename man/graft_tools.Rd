% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent-tools.R
\name{graft_tools}
\alias{graft_tools}
\title{Create bounded read-only tools for a Graft store or view}
\usage{
graft_tools(store, result_format = c("list", "json"))
}
\arguments{
\item{store}{An initialized \code{GraftStore} or immutable \code{GraftView}.}

\item{result_format}{Return ordinary R envelopes (\code{"list"}, the default), or
explicitly serialized envelopes (\code{"json"}) for ellmer and agent hosts.}
}
\value{
A named list of \code{ellmer::ToolDef} objects.
}
\description{
\code{graft_tools()} returns \code{\link[ellmer:tool]{ellmer::tool()}} definitions that delegate only
to Graft's public bounded retrieval operations. The tools do not expose SQL,
filesystem, network, connection, or mutation arguments.
Data-dict stores also expose \code{\link[=graft_dictionary]{graft_dictionary()}} for public metadata.
When the store has accepted definitions, \code{graft_definitions} exposes their
bounded catalog and one \code{graft_calculate} tool composes them through
\code{\link[=graft_calculate]{graft_calculate()}}. Both are omitted when no definitions are accepted.
When given a \code{GraftView}, all tools remain pinned to its immutable
snapshot boundary and the live-store integrity diagnostic is unavailable.
}
\details{
Every tool returns \code{result}, \code{truncated}, \code{limit}, and one canonical nested
\code{receipt}. The receipt identifies the store, exact accepted boundary, and
structural and build schema digests. Calculation receipts also identify the
complete accepted definition closure. Live-store tools pin a fresh boundary
for each invocation; tools created from a \code{GraftView} retain its snapshot
boundary.

Use \code{result_format = "json"} when registering tools with a chat. Each tool
then returns a \code{json}-class string containing the complete envelope, using
ellmer's supported explicit JSON return type. The default \code{"list"} preserves
direct R calls, including data frames and their attributes. It relies on
deprecated implicit conversion if registered with ellmer 0.5. The format is
fixed when tools are constructed and is the same inside and outside a chat.
}
\seealso{
\code{vignette("agents", package = "graft")} for pinning an accepted
boundary, registering the tools with a chat, and accepting
agent-authored proposals.
}
