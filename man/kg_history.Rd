% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/history.R
\name{kg_history}
\alias{kg_history}
\title{Retrieve the accepted history of one record}
\usage{
kg_history(store, id, as_of = NULL, limit = 100)
}
\arguments{
\item{store}{An initialized \code{kg_store}.}

\item{id}{One internal record identifier.}

\item{as_of}{Optional committed batch identifier or scalar \code{POSIXt} time.}

\item{limit}{Maximum number of revisions to return.}
}
\value{
A bounded data frame with \code{changed_fields} and \code{record} list-columns.
}
\description{
Revisions are returned in deterministic newest-first commit order. \code{as_of}
selects the state committed by a boundary: either an exact committed batch
identifier or a \code{POSIXt} time. Time boundaries are first resolved to a
committed batch order, so revision timestamps are never used as transaction
boundaries. With \code{limit = 1}, the returned \code{record} is the accepted record at
that boundary.
}
