% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artifact-store.R
\name{graft_artifact_store}
\alias{graft_artifact_store}
\alias{graft_artifact_save}
\alias{graft_artifact_read}
\title{Preserve immutable artifact content}
\usage{
graft_artifact_store(path, create = FALSE, max_bytes = 64 * 1024^2)

graft_artifact_save(store, id, bytes, media_type)

graft_artifact_read(store, ref)
}
\arguments{
\item{path}{Directory for a Graft artifact store, distinct from a native
graph store. Creation requires a missing or empty directory. Filesystem
paths follow platform limits, independently of artifact identity byte limits.}

\item{create}{Create a new store? Defaults to \code{FALSE} for safe reopening.}

\item{max_bytes}{Maximum payload bytes to save or read in this handle.}

\item{store}{A handle returned by \code{graft_artifact_store()}.}

\item{id}{Stable, nonempty artifact identity chosen by the caller, at most
1024 UTF-8 bytes, without padding or control characters.}

\item{bytes}{Raw vector containing the complete payload. Attributes are
discarded; only the byte contents are retained. Content is never
deserialized or executed by these functions.}

\item{media_type}{Nonempty media type describing the opaque payload, at most
1024 UTF-8 bytes, without padding or control characters.}

\item{ref}{Exact reference: a list with \code{id} and \code{revision} strings, as
returned by \code{graft_artifact_save()}.}
}
\value{
\code{graft_artifact_store()} returns a local store handle.
\code{graft_artifact_save()} returns a list containing \code{id} and \code{revision}.
\code{graft_artifact_read()} returns \code{ref}, \code{metadata} and verified raw \code{bytes}.
}
\description{
Create or reopen a local artifact store, save opaque bytes, and resolve an
exact revision. Saving grants no approval, access or execution authority.
}
\details{
Identity, media-type and reference strings are normalized to plain UTF-8
character values without R attributes.

This interface supports trusted local files with one writer. SHA-256 digests
identify content and metadata. Identical saves return the same reference;
corrections retain earlier revisions. There is no mutable latest pointer.

Payloads are published before immutable revision metadata, using staged files
in the destination directory. A failed save can leave unreferenced bytes;
retries reuse verified content. Orphans are retained rather than deleted
automatically. Successful reads verify metadata, payload size and digest.
Interrupted writes cannot yield a successful incomplete reference, but this
interface does not promise power-loss durability, concurrent publication,
authorization, erasure or backup recovery. Applications own access and
policy.
Handles contain no open connections and need no closing.
}
\examples{
path <- tempfile("artifacts-")
store <- graft_artifact_store(path, create = TRUE)
ref <- graft_artifact_save(
  store, "report:daily", charToRaw("A retained report"), "text/plain"
)
reopened <- graft_artifact_store(path)
rawToChar(graft_artifact_read(reopened, ref)$bytes)
unlink(path, recursive = TRUE)
}
