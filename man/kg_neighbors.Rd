% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph-traverse.R
\name{kg_neighbors}
\alias{kg_neighbors}
\title{Retrieve a bounded graph neighborhood}
\usage{
kg_neighbors(
  store,
  id,
  predicate = NULL,
  direction = c("both", "out", "in"),
  hops = 1,
  projection = c("semantic", "provenance", "combined"),
  max_nodes = 500,
  max_edges = 2000
)
}
\arguments{
\item{store}{An initialized \code{kg_store}.}

\item{id}{One projected graph node identifier.}

\item{predicate}{Optional exact predicate restriction applied at every hop.}

\item{direction}{One of \code{"both"}, \code{"out"}, or \code{"in"}.}

\item{hops}{One or two hops.}

\item{projection}{One of \code{"semantic"}, \code{"provenance"}, or \code{"combined"}.}

\item{max_nodes}{Maximum collected nodes, up to 500.}

\item{max_edges}{Maximum collected edges, up to 2,000.}
}
\value{
A collected \code{kg_subgraph} with nodes, edges, request metadata,
limits, truncation state, and the store structural digest.
}
\description{
\code{kg_neighbors()} performs deterministic breadth-first expansion for one or
two hops. It only follows generated graph projections and always collects a
bounded result.
}
