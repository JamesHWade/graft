% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph-traverse.R
\name{kg_traverse}
\alias{kg_traverse}
\title{Traverse a bounded predicate path}
\usage{
kg_traverse(
  store,
  from,
  via,
  direction = "out",
  max_hops = length(via),
  max_nodes = 500,
  max_edges = 2000,
  projection = "combined"
)
}
\arguments{
\item{store}{An initialized \code{kg_store}.}

\item{from}{One projected graph node identifier.}

\item{via}{One or two exact predicates in traversal order.}

\item{direction}{One of \code{"out"}, \code{"in"}, or \code{"both"}.}

\item{max_hops}{Maximum predicates from \code{via} to follow, up to two.}

\item{max_nodes}{Maximum collected nodes, up to 500.}

\item{max_edges}{Maximum collected edges, up to 2,000.}

\item{projection}{One of \code{"combined"}, \code{"semantic"}, or \code{"provenance"}.}
}
\value{
A collected \code{kg_subgraph} with path and limit metadata.
}
\description{
\code{kg_traverse()} follows a manifest-safe sequence of one or two predicates.
It uses generated joins for each hop and never runs recursive or unbounded
SQL.
}
