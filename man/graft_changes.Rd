% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/changes.R
\name{graft_changes}
\alias{graft_changes}
\title{List accepted changes between two boundaries}
\usage{
graft_changes(store, since = NULL, until = NULL, class = NULL, limit = 1000L)
}
\arguments{
\item{store}{An initialized \code{GraftStore} or immutable \code{GraftView}.}

\item{since}{Optional lower boundary, exclusive.}

\item{until}{Optional upper boundary, inclusive.}

\item{class}{Optional concrete class restriction.}

\item{limit}{Maximum changed records to return, up to the package hard
limit.}
}
\value{
A bounded data frame ordered by class and record ID with columns
\code{class}, \code{record_id}, \code{action} (\code{"insert"} when the record first appeared
inside the window, otherwise \code{"update"}), \code{revisions} (accepted revisions
inside the window), \code{changed_fields} (a list-column with the union of
public fields changed inside the window), and the \code{revision_id},
\code{revision_number}, \code{batch_id}, \code{commit_order}, \code{committed_at}, and public
\code{record} list-column of the latest revision at \code{until}. Attributes
\code{since_commit_order}, \code{since_batch_id}, \code{until_commit_order}, and
\code{until_batch_id} identify the compared boundaries.
}
\description{
\code{graft_changes()} compares accepted knowledge at two committed boundaries
and returns one row per record whose accepted revision differs between
them. It answers "what was accepted since this snapshot" for a whole store
in one bounded table, where \code{\link[=graft_history]{graft_history()}} answers the same question for
one record.
}
\details{
Each boundary may be a \code{\link[=graft_snapshot]{graft_snapshot()}} captured from the same store, a
committed batch ID, or a scalar \code{POSIXt} time. \code{since} defaults to the
store's origin, so every accepted record is reported as an insert. \code{until}
defaults to the store's current head, or to the pinned boundary of a
\code{GraftView}; a view rejects any later boundary.
}
