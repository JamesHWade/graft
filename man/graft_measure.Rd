% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measure-eval.R
\name{graft_measure}
\alias{graft_measure}
\title{Evaluate an accepted measure}
\usage{
graft_measure(store, name, arguments = list(), by = NULL)
}
\arguments{
\item{store}{An initialized \code{GraftStore} or immutable \code{GraftView}.}

\item{name}{The name of one accepted measure.}

\item{arguments}{A named list of values for declared parameters.}

\item{by}{Optional character vector of declared dimensions to group by.}
}
\value{
A data frame with one \code{value} column, preceded by one column per
\code{by} dimension, carrying \code{measure_id}, \code{revision_id}, and
\code{store_schema_digest} attributes.
}
\description{
\code{graft_measure()} evaluates one accepted measure over current accepted
state, or over the pinned boundary of a \code{GraftView}. Supplied arguments
bind to the measure's declared parameters as equality predicates, and
\code{by} may name only declared dimensions. Evaluation is read-only and
deterministic: the same boundary, definition, and arguments always
return the same answer.
}
