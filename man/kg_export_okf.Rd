% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/okf.R
\name{kg_export_okf}
\alias{kg_export_okf}
\title{Export accepted Graft knowledge as an Open Knowledge Format bundle}
\usage{
kg_export_okf(
  store,
  path = NULL,
  classes = NULL,
  as_of = NULL,
  limit = 5000,
  overwrite = FALSE
)
}
\arguments{
\item{store}{An initialized \code{kg_store}.}

\item{path}{Destination directory. The default uses the store's managed OKF
directory. It need not already exist.}

\item{classes}{Optional concrete classes to export. The default exports all
public classes in the active manifest.}

\item{as_of}{Optional committed batch identifier or scalar \code{POSIXt} time.
The default exports current accepted record heads.}

\item{limit}{Maximum number of concepts. An export that exceeds the limit
fails rather than writing a partial bundle.}

\item{overwrite}{Whether to replace an existing Graft-produced OKF bundle.}
}
\value{
A \code{kg_okf_bundle} summary.
}
\description{
\code{kg_export_okf()} writes a deterministic
\href{https://github.com/GoogleCloudPlatform/knowledge-catalog/tree/main/okf}{Open Knowledge Format}
(OKF) v0.2 directory from accepted Graft revisions. The bundle is a
human-readable projection for agents, Git, and documentation tools. The
active LinkML-derived manifest remains the executable contract for identity,
validation, storage, and retrieval.
}
\details{
Every concept includes a \code{graft} frontmatter extension with stable record,
revision, batch, and schema identity. Object references become Markdown
links, and direct or claim-evidence source references become OKF \code{sources}.
Sensitive slots remain excluded through the historical manifest that
governed each exported revision.

Exports are bounded and atomic. Existing directories are never replaced
unless \code{overwrite = TRUE} and the directory identifies itself as a
Graft-produced OKF bundle. The managed directory is reserved for a complete
projection of current accepted state; selected or historical exports must
use another \code{path}.
}
\examples{
\dontrun{
bundle <- kg_export_okf(store, "knowledge/okf")
bundle
}
}
