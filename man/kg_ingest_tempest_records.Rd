% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tempest-artifact-store.R
\name{kg_ingest_tempest_records}
\alias{kg_ingest_tempest_records}
\title{Ingest records mapped from one Tempest run}
\usage{
kg_ingest_tempest_records(
  store,
  run_id,
  records,
  stage = NULL,
  producer_version = NULL
)
}
\arguments{
\item{store}{An initialized, writable \code{kg_store}.}

\item{run_id}{One stable Tempest run identifier.}

\item{records}{A named list of mapped concrete-class data frames accepted by
\code{\link[=kg_ingest]{kg_ingest()}}.}

\item{stage}{Optional stable stage identifier, such as \code{"search"} or
\code{"synthesize"}.}

\item{producer_version}{Optional Tempest producer version.}
}
\value{
A \code{kg_ingest_result}. Replaying the same run and stage returns the
original committed result with \code{replay = TRUE} and signals
\code{graft_batch_replay}.
}
\description{
This is the durable Tempest-to-Graft knowledge handoff. Tempest maps its
domain objects to concrete Graft record data frames before calling this
function. The run identifier is both producer lineage and the default
idempotency key. A stage uses \verb{<run_id>:<stage>}, allowing independently
replayable stage commits.
}
\details{
This function is independent of Tempest's typed deliverable artifact-store
adapter.
}
