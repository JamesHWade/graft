% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artifact-store.R
\name{graft_with_store_lock}
\alias{graft_with_store_lock}
\title{Hold a store's lock while an application changes it}
\usage{
graft_with_store_lock(store, code, exclusive = TRUE)
}
\arguments{
\item{store}{A handle returned by \code{\link[=graft_store]{graft_store()}} or
\code{\link[=graft_store_postgres]{graft_store_postgres()}}.}

\item{code}{Code to run while the lock is held.}

\item{exclusive}{\code{TRUE} to keep every other process out of the store,
\code{FALSE} to share the lock with other writers.}
}
\value{
The value of \code{code}. If the lock is not free within the store's
\code{lock_timeout}, an error of class \code{graft_store_busy_error} is signalled
and \code{code} is not run.
}
\description{
Run code while holding the store's lock, so an application can make a
change that spans several Graft calls without another process writing in
between. Every write to a local store holds this lock shared, and
\code{\link[=graft_manifest]{graft_manifest()}}, \code{\link[=graft_plan_replacement]{graft_plan_replacement()}}, \code{\link[=graft_replace]{graft_replace()}},
\code{\link[=graft_backup]{graft_backup()}}, and \code{\link[=graft_restore]{graft_restore()}} hold it exclusively.
}
\details{
A host that forgets an artifact by building a replacement store holds the
old store's lock exclusively while it plans, copies, and switches to the
replacement, and wraps each of its writes in a shared hold that first checks
the store is still current. A write that was waiting on the lock then sees
the switch and goes to the new store instead of the retired one.

Graft calls inside \code{code} run under the lock already held. An exclusive
hold cannot be taken inside a shared one. PostgreSQL stores run \code{code}
directly: the host's transaction and scope lock already serialize it.
}
\examples{
path <- tempfile("artifacts-")
store <- graft_store(path, create = TRUE)
ref <- graft_with_store_lock(store, graft_save(store, "Evidence", "report"))
graft_read(store, ref)@data
unlink(path, recursive = TRUE)
}
