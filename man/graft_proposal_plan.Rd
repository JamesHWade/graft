% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proposals.R
\name{graft_proposal_plan}
\alias{graft_proposal_plan}
\title{Turn raw structured proposals into a reviewable plan}
\usage{
graft_proposal_plan(store, proposal, provenance, max_rows = 100L)
}
\arguments{
\item{store}{An initialized \code{GraftStore} with a data-dict contract.}

\item{proposal}{A named list of table arrays, each an unnamed list of named
record objects. JSON arrays must remain lists, including primitive lists.}

\item{provenance}{A \code{\link[=graft_provenance]{graft_provenance()}} object with explicit idempotency.}

\item{max_rows}{Maximum records per table, from 1 to 1,000. Set this to the
same value used for \code{\link[=graft_proposal_type]{graft_proposal_type()}}.}
}
\value{
A \code{GraftCommitPlan} from \code{\link[=graft_plan]{graft_plan()}}, without changing accepted data.
}
\description{
\code{graft_proposal_plan()} checks the raw object shape, preserves each row and
value in list-columns, and delegates acceptance validation to \code{\link[=graft_plan]{graft_plan()}}.
It never commits, calls a provider, or requires ellmer. Supply the unconverted
result of \code{chat_structured(..., convert = FALSE)}; converted data frames are
rejected so upstream conversion cannot hide unknown fields or malformed rows.
}
\details{
Malformed objects, unknown or restricted fields, and incompatible JSON value
types raise a \code{graft_validation_error}. Missing required values, invalid enum
values, and unknown references are reported in the returned plan's \verb{@issues}.
These are complete candidate records, not patches: omitted optional values
are missing values under ordinary planning semantics. The host retains review,
provenance, idempotency, credentials, and the decision to \code{\link[=graft_commit]{graft_commit()}}.
Retain the accepted plan to retry its commit. Replanning after acceptance
creates a different plan and cannot reuse an already committed replay key.
}
