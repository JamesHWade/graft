% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{kg_connect_duckdb}
\alias{kg_connect_duckdb}
\title{Connect to a DuckDB knowledge store}
\usage{
kg_connect_duckdb(
  schema,
  path = ":memory:",
  read_only = FALSE,
  connection = NULL,
  okf = c("managed", "disabled"),
  okf_path = NULL
)
}
\arguments{
\item{schema}{A \code{kg_schema} object or manifest path.}

\item{path}{DuckDB file path, or \code{":memory:"}. When supplied together with
\code{connection}, it must identify that connection's database.}

\item{read_only}{Whether the store must prohibit writes.}

\item{connection}{An optional existing DuckDB DBI connection.}

\item{okf}{Whether to manage an Open Knowledge Format working tree for the
store. File-backed stores use \code{"managed"} by default. Use \code{"disabled"} to
opt out.}

\item{okf_path}{Optional managed OKF directory. By default, a file-backed
\code{knowledge.duckdb} store uses the sibling \code{knowledge.okf} directory.}
}
\value{
A \code{kg_store} object. Call \code{\link[=kg_init]{kg_init()}} before using a new store.
}
\description{
A store combines a compiled graft schema with one DuckDB connection. When
graft creates the connection, graft owns and closes it. A caller-supplied
connection is never closed by graft.
}
