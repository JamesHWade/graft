% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graft-retrieval.R
\name{graft_query}
\alias{graft_query}
\title{Run a bounded advanced retrieval operation}
\usage{
graft_query(
  store,
  operation = c("lookup", "identifiers", "claims", "evidence", "neighbors", "traverse",
    "unresolved", "integrity"),
  request = list(),
  limit = 100L
)
}
\arguments{
\item{store}{An initialized \code{GraftStore} or immutable \code{GraftView}.}

\item{operation}{One supported operation name.}

\item{request}{A named list of operation-specific values.}

\item{limit}{Maximum rows for tabular operations.}
}
\value{
An ordinary bounded data frame or list, depending on the operation.
}
\description{
\code{graft_query()} is the advanced read-only retrieval surface. It accepts a
named \code{request} rather than SQL. Supported operations are \code{"lookup"},
\code{"identifiers"}, \code{"claims"}, \code{"evidence"}, \code{"neighbors"}, \code{"traverse"},
\code{"unresolved"}, and \code{"integrity"}.
}
\details{
Request members are operation-specific: exact identifier lookup accepts
\code{namespace}, \code{value}, and optional \code{class}; identifiers and claims accept
\code{id}; evidence accepts \code{statement_id} or \code{source_id}; graph operations
accept their bounded path and projection arguments; unresolved mentions
accept optional \code{class} and \code{source_id}; and integrity accepts \code{projections}.
Unknown members are rejected. Tabular results carry \code{limit}, \code{truncated},
and \code{store_schema_digest} attributes.
A \code{GraftView} pins every operation and does not support \code{"integrity"}.
}
