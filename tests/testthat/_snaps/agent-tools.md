# explicit JSON tools retain the direct envelope and tool contract

    Code
      graft_tools(view, result_format = "js")
    Condition
      Error in `graft_tools()`:
      ! `result_format` must be one of "list" or "json", not "js".
      i Did you mean "json"?

