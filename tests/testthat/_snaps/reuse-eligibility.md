# consultation refuses when access or eligibility changes between checks

    Code
      result <- tool()
    Condition
      Error:
      ! Reuse is not authorized.

---

    Code
      result <- tool()
    Condition
      Error:
      ! Reuse is not authorized.

