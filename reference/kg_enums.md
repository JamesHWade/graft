# List enum values in a graft schema

List enum values in a graft schema

## Usage

``` r
kg_enums(schema)
```

## Arguments

- schema:

  A `kg_schema` object or manifest path.

## Value

A data frame with one row per permissible enum value.
