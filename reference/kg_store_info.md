# Inspect a graft store

Inspect a graft store

## Usage

``` r
kg_store_info(store)
```

## Arguments

- store:

  A `kg_store` object.

## Value

A named list describing the connection, initialization state, and active
schema fingerprints.
