# List external identifiers for one record

List external identifiers for one record

## Usage

``` r
kg_identifiers(store, id)
```

## Arguments

- store:

  An initialized `kg_store`.

- id:

  One internal record identifier.

## Value

A bounded data frame of public identifier-registry details.
