# Report DuckDB store capabilities

Report DuckDB store capabilities

## Usage

``` r
kg_capabilities(store)
```

## Arguments

- store:

  A `kg_store` object.

## Value

A named list of static backend and connection capabilities.
