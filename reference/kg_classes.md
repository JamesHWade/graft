# List concrete classes in a graft schema

List concrete classes in a graft schema

## Usage

``` r
kg_classes(schema)
```

## Arguments

- schema:

  A `kg_schema` object or manifest path.

## Value

A data frame with one row per concrete class.
